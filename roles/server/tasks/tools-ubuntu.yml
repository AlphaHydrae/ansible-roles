---
- name: apt-get install tools
  apt: pkg=${item} state=latest
  with_items:
    - build-essential
    - curl
    - python-setuptools
    - rsync
    - vim
    - wget
    - zsh
  tags:
    - tools
